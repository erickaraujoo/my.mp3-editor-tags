import 'dotenv/config';

export const SERVER = {
  PORT: 3333,
  NODE_ENV: 'development',
};

export const LINK = {
  BASE_URL_IMAGE: 'http://localhost:3333/files/',
};

export const CONFIG = {
  USER: 'Users/win',
  PATH: 'Music/editor-tag',
  LOCAL_DISK: 'C:',
  FORMAT: 'mp3',
  RESOLUTION: '800x800'
}